/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as DevRev from "../index";

export interface Meeting extends DevRev.AtomBase {
    /** The artifacts attached to the meeting. */
    artifacts?: DevRev.ArtifactSummary[];
    channel?: DevRev.MeetingChannel;
    /** Custom fields. */
    customFields?: Record<string, unknown>;
    /** Custom schema fragments. */
    customSchemaFragments?: string[];
    /** Description of the meeting. */
    description?: string;
    /** Time at which meeting ended. */
    endedDate?: Date;
    /**
     * External reference of the meeting. This is the identifier from the
     * meeting channel/provider.
     */
    externalRef?: string;
    /** External URL associated with the meeting. */
    externalUrl?: string;
    /** The members in the meeting. */
    members: DevRev.UserSummary[];
    organizer?: DevRev.UserSummary;
    parent?: DevRev.AtomSummary;
    /** Recording URL of the meeting. */
    recordingUrl?: string;
    /** Time at which meeting was scheduled to start. */
    scheduledDate?: Date;
    state?: DevRev.MeetingState;
    /** Stock schema fragment. */
    stockSchemaFragment?: string;
    /** Subtype corresponding to the custom type fragment. */
    subtype?: string;
    /** Tags associated with the meeting. */
    tags?: DevRev.TagWithValue[];
    /** Title of the meeting object. */
    title?: string;
}
