/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as DevRev from "../index";

export interface EmailPreviewWidget extends DevRev.SnapWidgetBase {
    /** The list of bcc addresses. */
    bcc: DevRev.EmailInfo[];
    /** The list of cc addresses. */
    cc: DevRev.EmailInfo[];
    /** The list of from addresses. */
    from: DevRev.EmailInfo[];
    /** The html body of the email. */
    htmlBody?: string;
    /** The in-reply-to header of the email. */
    inReplyTo?: string;
    /** The list of inline attachments. */
    inlines: DevRev.EmailInlineAttachment[];
    /** Whether the email is spam. */
    isSpam?: boolean;
    /** The message id of the email. */
    messageId?: string;
    rawEmailArtifact?: DevRev.ArtifactSummary;
    /** The references header in the email. */
    references: string[];
    /** The list of reply to addresses. */
    replyTo: DevRev.EmailInfo[];
    /** The time the email was sent. */
    sentTimestamp?: Date;
    /** The subject of the email. */
    subject?: string;
    /** The text body of the email. */
    textBody?: string;
    /** The list of to addresses. */
    to: DevRev.EmailInfo[];
}
